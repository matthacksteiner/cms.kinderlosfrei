name: Button Group
icon: grid

tabs:
  content:
    label: Inhalt
    fields:
      buttons:
        type: structure
        label: Buttons
        empty: Keine Buttons angelegt
        fields:
          linkObject:
            extends: fields/linkField

  settings:
    label: Einstellungen
    fields:
      info:
        label: Info
        type: info
        text: |
          Die Darstellung aller Button wird im **Tab** Design festgelegt.
      buttonAlign:
        extends: fields/align
        width: 1/4
      buttonLine:
        type: line
      buttonLocal:
        extends: fields/buttonLocal
      meta: fields/metadata
