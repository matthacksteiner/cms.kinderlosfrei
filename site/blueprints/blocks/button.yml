name: Button
icon: url

tabs:
  content:
    label: Inhalt
    fields:
      link:
        type: link
        label: Link
        required: true
        files:
          query: page.files.filterBy('template', 'docs')
  settings:
    label: Einstellungen
    fields:
      info:
        label: Info
        type: info
        text: |
          Die Darstellung aller Button wird im **Tab** Design festgelegt.
      buttonAlign:
        extends: fields/align
        width: 1/4
