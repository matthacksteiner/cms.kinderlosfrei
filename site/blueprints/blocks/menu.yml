name: Navigations Menü
icon: menu
wysiwyg: false

tabs:
  content:
    label: Inhalt
    fields:
      nav:
        label: Footer
        type: structure
        fields:
          link:
            type: link
            label: Link
            required: true
            pages:
              query: site.index.filterBy('intendedTemplate', 'in', ['default', 'home'])
            linkTypes:
              - page
              - url
            settings:
              text:
                required: true
  settings:
    label: Einstellungen
    fields:
      listHeadline:
        label: Liste
        type: headline
        numbered: false
      listGap:
        label: Listen Abstand
        type: number
        width: 1/3s
        default: 16
        after: px
      gap:
        type: gap
        width: 2/3
      textHeadline:
        label: Text
        type: headline
        numbered: false
      textGroup: groups/textGroup
