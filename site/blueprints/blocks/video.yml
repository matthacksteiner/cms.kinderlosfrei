name: Video
icon: video
preview: video

tabs:
  content:
    label: Inhalt
    fields:
      source:
        type: toggles
        label: Video Quelle
        options:
          - value: remote
            text: Externer Link
            icon: url
          - value: local
            text: Video Datei
            icon: file
      url:
        label: field.blocks.video.url.label
        type: url
        placeholder: field.blocks.video.url.placeholder
        help: YouTube (Format https://www.youtube.com/watch?v=vH1poPSIvOk) und Vimeo (Format https://vimeo.com/22775048) Links werden unterstützt.
        when:
          source: remote
      caption: false
      file:
        label: Video-Datei
        type: files
        query: page.videos.filterBy('extension', 'mp4')
        multiple: false
        uploads:
          template: video
        when:
          source: local
  settings:
    label: Einstellungen
    fields:
      ratioHeadline:
        label: Seitenverhältnis
        type: headline
        numbered: false
      ratioMobile:
        extends: fields/ratio
        label: Seitenverhältnis 📱
        width: 1/2
        when:
          source: remote
      ratio:
        extends: fields/ratio
        label: Seitenverhältnis 🖥️
        width: 1/2
        when:
          source: remote
      optionsHeadline:
        label: Wiedergabe
        type: headline
        numbered: false
      options:
        type: checkboxes
        label: Optionen
        width: 1/1
        columns: 4
        default:
          - autoplay
          - loop
          - muted
        options:
          autoplay: Autoplay
          loop: Loop
          muted: Stumm
          controls: Steuerung
        help: "Auotplay deaktiviert den Ton bei der Wiedergabe."
