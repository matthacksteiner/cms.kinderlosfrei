name: Video
icon: video
preview: video

tabs:
  content:
    label: Inhalt
    fields:
      source:
        type: toggles
        label: Video Quelle
        options:
          - value: remote
            text: Externer Link
            icon: url
          - value: local
            text: Video Datei
            icon: file
      url:
        label: field.blocks.video.url.label
        type: url
        placeholder: field.blocks.video.url.placeholder
        help: YouTube und Vimeo Links werden unterstützt.
        when:
          source: remote
      caption: false
      file:
        label: Video-Datei
        type: files
        query: page.videos.filterBy('extension', 'mp4')
        multiple: false
        uploads:
          template: video
        when:
          source: local
  settings:
    label: Einstellungen
    fields:
      options:
        type: checkboxes
        label: Wiedergabe Optionen
        width: 1/3
        default:
          - autoplay
          - loop
          - muted
        options:
          autoplay: Autoplay
          loop: Loop
          muted: Stumm
          controls: Steuerung
        help: "Auotplay deaktiviert den Ton bei der Wiedergabe."
      ratioMobile:
        extends: fields/ratio
        label: Seitenverhältnis 📱
        width: 1/3
        when:
          source: remote
      ratio:
        extends: fields/ratio
        label: Seitenverhältnis 🖥️
        width: 1/3
        when:
          source: remote
