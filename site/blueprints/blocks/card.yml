name: Karte
icon: copy

tabs:
  content:
    label: Inhalt
    fields:
      title:
        label: Titel
        type: writer
        extends: fields/writer
        marks: false
        nodes: false
        inline: true
        placeholder: field.blocks.heading.placeholder
      text:
        label: Text
        type: writer
        extends: fields/writer
        marks: false
        nodes: false
        inline: true
        placeholder: field.blocks.text.placeholder
      image:
        label: SVG Icon
        type: files
        icon: image
        layout: cards
        multiple: false
        back: white
        query: page.images.filterBy('extension', 'svg')
        uploads:
          template: svg
      hoverToggle:
        label: Hover Effekt
        type: toggle
        default: false
      hovertext:
        extends: fields/hover
        when:
          hoverToggle: true
      hoverimage:
        extends: fields/hover
        when:
          hoverToggle: true

  settings:
    label: Einstellungen
    fields:
      titleHeadline:
        label: Titel Einstellungen
        type: headline
        numbered: false
      titleGroup: groups/titleGroup
      textHeadline:
        label: Text Einstellungen
        type: headline
        numbered: false
      textGroup: groups/textGroup
      sizeHeadline:
        label: Icon Einstellungen
        type: headline
        numbered: false
      widthMobile:
        label: Größe 📱
        extends: fields/width
      widthDesktop:
        label: Größe 🖥️
        extends: fields/width
      widthAlign:
        label: Ausrichtung
        extends: fields/align
        width: 1/4
      meta: fields/metadata
