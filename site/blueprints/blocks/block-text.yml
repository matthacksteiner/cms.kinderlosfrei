name: Text
icon: text
preview: textCustom

tabs:
  content:
    label: Inhalt
    fields:
      textOptions: fields/textOptions
      text:
        label: Text
        required: true
        extends: fields/textarea
  config: tabs/config
