name: Bilder
icon: image
preview: image

tabs:
  content:
    label: Inhalt
    fields:
      ratio:
        width: 1/4
        extends: fields/ratio
      align:
        extends: fields/align
        width: 1/4
      layout:
        width: 2/4
        extends: fields/layout
      images:
        extends: fields/images
        max: 4
  config: tabs/config
