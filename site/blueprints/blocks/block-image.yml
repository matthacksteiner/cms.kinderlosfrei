name: Bilder
icon: image
preview: image

tabs:
  content:
    label: Inhalt
    fields:
      ratio:
        width: 1/2
        extends: fields/ratio
      width:
        label: Verhältnis
        type: select
        width: 1/2
        default: "equal"
        options:
          - value: "equal"
            text: "gleich"
          - value: "big-small"
            text: "groß | klein"
          - value: "small-big"
            text: "klein | groß"

      images:
        extends: fields/images
  config: tabs/config
